storage:
  landing: "data/landing"
  bronze: "data/bronze"
  silver: "data/silver"
  gold: "data/gold"
  quarantine: "data/quarantine"
  archive: "data/archive"

datasets:
  patient_admissions: "patient_admissions"
  dim_hospital: "dim_hospital"
  dim_department: "dim_department"
  dim_patient: "dim_patient"
  dim_date: "dim_date"
  fact_patient_admissions: "fact_patient_admissions"

logging:
  level: INFO

watermark:
  file: "data/watermark.json"

postgres:
  host: 127.0.0.1
  port: 5433
  database: nhs_healthcare
  user: nhs_user
  password: YOUR_POSTGRES_PASSWORD
  table: patient_admissions
  jdbc_driver: org.postgresql.Driver